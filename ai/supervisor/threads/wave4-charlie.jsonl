{"ts":"2026-04-23T00:00:00Z","kind":"note","agent":"implementer-charlie","msg":"plan: (1) types.ts — add ConfigurableField TS type + Node.configurable Zod schema in schemas.ts; (2) document triggerPayload.configurable convention via TriggerPayload type alias / JSDoc (TriggerPayload remains z.unknown() for back-compat — extracted at runtime via duck typing); (3) executor.ts — add resolveConfigurable() helper that merges in precedence: triggerPayload.configurable[k] → node.config[k] → node.configurable[k].default; (4) call helper at invokeNode entry BEFORE ctx-attachment block ~line 885, pass merged map; (5) attach merged map to ctx.nodeConfig (replaces today's raw node.config attachment); (6) memoization key — at the SOLE call site executor.ts:807 (step.run(node.id, ...)), suffix the step name with a deterministic hash of the resolved configurable values when at least one override is present (otherwise keep just node.id for back-compat). Hash via crypto.createHash('sha256').update(stableJsonStringify(resolved)).digest('hex').slice(0,12); (7) tests — add executor.configurable.test.ts covering 5 cases per acceptance criteria"}
