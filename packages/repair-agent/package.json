{
  "name": "@chorus/repair-agent",
  "version": "0.1.0",
  "description": "Claude-powered repair agent — proposes integration patches, validates via snapshots",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@chorus/core": "*",
    "zod": "^3.23.0"
  }
}
