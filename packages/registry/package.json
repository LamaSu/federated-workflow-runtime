{
  "name": "@chorus/registry",
  "version": "0.1.0",
  "description": "Signed patch registry — git-backed, Ed25519 signatures, canary ladder",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@chorus/core": "*",
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.5.0",
    "simple-git": "^3.27.0",
    "zod": "^3.23.0"
  }
}
